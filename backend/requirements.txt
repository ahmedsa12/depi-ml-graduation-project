# Real-Time Sign Language Recognition — backend dependencies
# Python 3.10–3.12

fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart/form-data (file uploads)
pydantic>=2.7

# ML runtime — must match what the notebook trained with
tensorflow>=2.15
mediapipe>=0.10.35
protobuf>=5.28
scikit-learn>=1.3
joblib>=1.3
numpy>=1.26
opencv-python-headless>=4.9    # headless: no GUI libs needed on a server
